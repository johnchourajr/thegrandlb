/**
 * Venue contact details.
 *
 * The number is currently repeated as a literal in several content files
 * (contact, accessibility, [uid], JsonLdLocalBusiness, shared.constants). This
 * is not yet the single source for those — it exists so the inquiry form does
 * not add a sixth copy. Consolidating the rest is worth doing separately.
 */
export const VENUE_PHONE_DISPLAY = "562.426.0555";
export const VENUE_PHONE_HREF = "tel:+15624260555";
